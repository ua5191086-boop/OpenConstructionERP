// Auto-generated silent check SSO page for Keycloak
// This page loads in an iframe to check the Keycloak session silently.
// It must be served at the origin of the SPA.

import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <div style={{ display: 'none' }}>Silent SSO check</div>
  </StrictMode>
)